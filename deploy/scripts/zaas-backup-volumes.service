[Unit]
Description=ZaaS Docker volume backup (GFS rotation)
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
User=deploy
Group=deploy
ExecStart=/opt/zaas/deploy/scripts/backup-volumes.sh
StandardOutput=journal
StandardError=journal
